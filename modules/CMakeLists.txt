if(CLENCH_ENABLED_MODULES)
    foreach(i ${CLENCH_ENABLED_MODULES})
        add_subdirectory(${i})
    endforeach()
else()
    file(GLOB DIRS *)
    foreach(i ${DIRS})
        if(IS_DIRECTORY ${i})
            add_subdirectory(${i})
        endif()
    endforeach()
endif()
